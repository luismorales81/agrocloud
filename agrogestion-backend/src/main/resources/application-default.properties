# Perfil default: desarrollo local. Hereda defaults de application.properties (localhost, root, sin contraseña).
# Para producción usar siempre perfil prod y definir variables en el entorno.
# Opcional: definir DATABASE_URL, DATABASE_USERNAME, DATABASE_PASSWORD en .env (nunca subir .env al repo).
